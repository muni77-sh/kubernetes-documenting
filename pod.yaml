apiVersion; v1
kind: pod
metadata:
  creationTimestamp: null 
  labels:
    run: redis
  name: redis
spec:
  containers:
  - image: redis
    name: redis
    resources: {}
  dnsPolicy; ClusterFirst
  restartPolicy: Always
status: {}
